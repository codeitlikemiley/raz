load("@complex_bazel_setup_crates//:defs.bzl", "all_crate_deps")
load("@rules_rust//rust:defs.bzl", "rust_binary")

rust_binary(
    name = "complex-bazel-setup",
    srcs = glob(["src/**/*.rs"]),
    deps = all_crate_deps(normal = True),
    visibility = ["//visibility:public"],
)
